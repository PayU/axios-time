{
    "name": "axios-time",
    "version": "1.0.0",
    "description": "Axios plugin to measure the actual time it takes for a request to complete",
    "main": "src/index.js",
    "scripts": {
        "test": "$(npm bin)/mocha ./tests/test",
        "test-with-coverage": "$(npm bin)/nyc mocha ./tests/test"
    },
    "author": "PayU",
    "license": "Apache-2.0",
    "files": [
        "src"
    ],
    "devDependencies": {
        "chai": "^4.2.0",
        "axios": "^0.20.0",
        "mocha": "^8.1.2",
        "nyc": "^15.1.0"
    }
}
